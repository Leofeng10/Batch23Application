package com.example.GoogleCalendar;

/**
 * call when switching between month, the day interface should change
 */
public interface MonthChangeListner {
    void onmonthChange(MonthModel monthModel);
}
